import { PostedDurationPipe } from './posted-duration.pipe';

describe('PostedDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new PostedDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
